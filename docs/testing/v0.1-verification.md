# V0.1 verification

Date: 2026-09-25. Scope: V0.1 authenticated branch workspace, tracked in [FOUND-001 / #1](https://github.com/GnaneswarRaju/SriNidhi/issues/1). Branch: `feature/v0.1-foundation`. Status: local checks passed; review and connected-backend acceptance remain open. No production release or tag.

## Implemented

- `app/lib`: Supabase Auth adapter, platform session storage, guarded routes, verified branch selection, Drift preference persistence, responsive shell, configuration/error handling and sanitized logs.
- `supabase/migrations/20260924000100_foundation.sql`: businesses, branches, profiles, memberships, six branch roles, immutable audit events, RLS and a caller-scoped branch RPC. No financial tables or posting endpoints yet.
- `app/test`, `supabase/tests`, `.github/workflows`, `scripts`: repeatable application and authorization checks.
- Root documents and `docs/`: architecture decisions, future ledger/OCR/offline contracts, deployment/recovery guidance and Figma references.

## Local evidence

Windows; Flutter 3.47.5, Dart 3.13.4; locked dependencies. Commands run from `app/` unless stated otherwise.

| Check | Result |
|---|---|
| `flutter pub get`, `dart run build_runner build`, `flutter gen-l10n` | Passed; lockfile, Drift code and localized strings generated |
| `dart format lib test tool` | Passed |
| `flutter analyze --fatal-infos` | Passed; no issues |
| `flutter test --coverage` | 13 tests passed |
| `flutter test tool/render_screens_test.dart --dart-define=FLUTTER_SDK_PATH=<SDK path>` | 3 render cases passed; 6 PNGs inspected |
| `flutter build web --release --no-web-resources-cdn` | Passed; release output in ignored `app/build/web` |
| `node scripts/verify-database.mjs` (repository root) | Passed on actual PostgreSQL through PGlite with an Auth shim |
| Release app in Codex browser | Setup screen rendered; protected `#/workspace` redirected to setup without configuration; no warning/error console entries observed |
| Drift worker/WASM SHA-256 | Matched committed checksums |

Application tests cover public configuration/key validation, signed-out route guards, log payload exclusion, SQLite close/reopen and user isolation, stale/foreign branch rejection, authorized selection persistence, failure-closed membership loading, empty access, sign-in/out at widths 320/390/834/1440 and 200% text scaling at width 320.

Database assertions cover cross-business foreign-key rejection, tenant/branch read isolation, caller-scoped RPC results, cashier/owner audit visibility, denied role escalation/audit forgery, membership revocation, immutable audit deletion/truncation and anonymous denial. The PGlite Auth shim does not validate the real Supabase Auth service, PostgREST or Storage.

Screenshots in `docs/design/screenshots/` use clearly synthetic **test-only** accounts and branches. They are not evidence of hosted authentication or live store data. The initial screenshot helper needed real asynchronous font/image work and explicit Material icon font loading; both were fixed before final captures. The local preview's root-path check was corrected and verified with HTTP 200 and a browser render.

## Remaining acceptance and release gates

- Connect the selected Supabase development project, inspect its schema/history, apply the reviewed migration and provision the first authorized user. The user is connecting this project; no remote migration has been applied.
- Verify real password authentication, session restoration after restart, expiry, sign-out, revoked membership and browser preference persistence against that project.
- Run GitHub Actions on the pushed feature PR. Record actual run links/results below; authored workflows alone are not passing CI evidence.
- Android SDK/device and macOS/Xcode checks, secure-storage behavior and release signing remain unverified. Default development launcher icons remain; native production packaging is not complete.
- Figma layout references and rendered Flutter screens are linked in `docs/design/foundation.md`; documented component/state differences and final design review remain open.
- Configure and verify `main` branch protection. No production backend, deployment, release tag, offline financial posting, OCR provider or PostHog SDK was introduced.

## GitHub evidence

Initial local documentation commit: `04c485a`. The remote's existing `de36326` initial README commit is preserved. Implementation commit, PR and CI results are recorded after the push.

Next: close the connected-backend/review gates for V0.1, then begin V0.2 catalogue, exact unit conversions and stock ledger tests.
